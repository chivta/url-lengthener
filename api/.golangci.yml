run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
  disable:
    - exhaustruct

linters-settings:
  goimports:
    local-prefixes: github.com/arvlas/url-shortener
